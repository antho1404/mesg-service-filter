name: service-filter
sid: service-filter
description: 'Filter ther value'
tasks:
  filter:
    inputs:
      data:
        type: Any
      predicate:
        type: String
      value:
        type: Any
      state: 
        type: Any
    outputs:
      match:
        type: String
      data:
        type: Object